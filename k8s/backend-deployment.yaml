apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend-container
        # 👇 여기에 본인 Docker Hub 아이디를 꼭 넣으세요! (예: OhSeungUk)
        image: osw1014/my-k8s-project-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000