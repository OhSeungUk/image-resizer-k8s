name: CI - Build and Push Docker Images

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # [추가 1] QEMU 설정 (인텔 CPU가 ARM 요리를 할 수 있게 해주는 도구)
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    # Docker Hub 로그인
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    # [수정] 백엔드 빌드 (platforms 옵션 추가)
    - name: Build and push Backend
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: true
        # 👇 여기가 핵심! 두 가지 버전을 다 만들라고 지시
        platforms: linux/amd64,linux/arm64
        tags: ${{ secrets.DOCKER_USERNAME }}/my-k8s-project-backend:latest

    # [수정] 프론트엔드 빌드 (platforms 옵션 추가)
    - name: Build and push Frontend
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: true
        # 👇 여기도 추가
        platforms: linux/amd64,linux/arm64
        tags: ${{ secrets.DOCKER_USERNAME }}/my-k8s-project-frontend:latest